__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
.env.local
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
*.log

# project-specific
*.db
*.db-journal
*.db-wal
*.db-shm
# everything under data/ is generated/derived state (DBs, logs, model weights,
# venvs, photo-corpus symlink, sample dirs, thumbnail caches, …); none of it
# belongs in the repo.
data/
report*/

# editor / agent state
.claude/

# JS bundler dev-deps (esbuild). package.json + package-lock.json are committed;
# node_modules is regenerated by `npm install`.
node_modules/
